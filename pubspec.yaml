name: getx_todo_app
description: "GetX Todo App - Flutter 상태관리 학습 프로젝트"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # GetX 상태관리
  get: ^4.6.6
  validators: ^3.0.0
  intl: ^0.20.2


  # iOS 스타일 아이콘
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # 코드 린팅
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true